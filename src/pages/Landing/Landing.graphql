mutation landingForm($landingRequestInput: LandingRequestInput!) {
  landingRequest(landingRequestInput: $landingRequestInput) {
    runtimeError {
      exception
      message
    }
    fieldErrors {
      field
      message
    }
  }
}