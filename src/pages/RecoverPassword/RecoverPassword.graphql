mutation changePasswordByToken($email: String!, $token: String!, $password: String!) {
  changePasswordByToken(email: $email, token: $token, password: $password) {
    runtimeError {
      exception
      message
    }
    fieldErrors {
      field
      message
    }
    response
  }
}